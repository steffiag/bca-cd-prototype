npm install axios cors dotenv express express-session fs googleapis multer mysql2 nodemailer openai passport passport-google-oauth20 path react react-dom resend sequelize string-similarity

to run backend: node server.js
to run frontend: npm run dev
